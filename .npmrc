# npm security settings

# Require package-lock.json for reproducible builds
package-lock=true

# Run security audit on install
audit=true

# Use strict SSL for all registry connections
strict-ssl=true

# Prevent accidental publishing to npm
access=restricted

# Save exact versions (not ranges) when installing
save-exact=true
