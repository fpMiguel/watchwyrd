# Load Test Configuration with Worker Threads

NODE_ENV=production
PORT=7000
HOST=0.0.0.0
BASE_URL=http://localhost:7000
SECRET_KEY=watchwyrd-load-test-secret-key-12345678
LOG_LEVEL=info

# Cache settings (optimized for load testing)
CACHE_TTL=21600
CACHE_MAX_SIZE=10000

# Worker count (auto-detect optimal count if not set)
# WORKER_COUNT=6

# Rate limiting disabled for load testing
RATE_LIMIT_ENABLED=false

# Mock mode for testing (no real API calls)
MOCK_MODE=true
